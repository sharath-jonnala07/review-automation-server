app = "pulse-agent-gmail"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[processes]
  app = "uvicorn mcp_servers.smtp_server:app --host 0.0.0.0 --port 5001"

[env]
  PYTHONPATH = "/app"

[[vm]]
  memory = "512mb"
  cpu_kind = "shared"
  cpus = 1